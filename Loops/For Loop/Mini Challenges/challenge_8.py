# Loop through numbers 1 to 30
for i in range(1, 31):

    # Check if number is divisible by 4
    if i % 4 == 0:
        print(f"Divisible by 4: {i}") # Print if divisible

    # If not divisible by 4
    else:
        print(f"Number: {i}") # Print normal number